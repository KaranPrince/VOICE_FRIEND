// Constant for SIRIWAVE WIDGET
class PlayWidgetConstant {
  static const Duration intensityUpdateInterval = Duration(milliseconds: 50);
  static const double intensityDivisor = 10.0;
  static const double intensityMin = 0.05;
  static const double intensityMax = 1.0;
}
